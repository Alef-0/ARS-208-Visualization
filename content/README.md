# Reference content

This folder contains extracted page ranges from the Continental ARS40X
technical documentation used to interpret radar configuration, cluster,
quality, object, and warning CAN messages.

The filenames retain their original page ranges so code-to-document checks can
refer to the relevant extract directly.
